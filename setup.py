from setuptools import setup, find_packages

setup(
    name='src',
      version='1.0',
      description='Wine Quality with MLOPS',
      author='Rukshana',
      packages = find_packages(),
      license='MIT')

